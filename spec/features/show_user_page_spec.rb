require 'rails_helper'

describe "how a user can get to their individual page" do
  # it "will take the user to their own page" do
  #   user = FactoryGirl.create(:user)
  #   sign_in(user)
  #   click_on "My Profile"
  #   expect(page).to have_content "profile page"
  # end
end
